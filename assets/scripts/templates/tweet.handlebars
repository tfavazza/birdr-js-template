<h1> <small>Tweet from: {{Object.tweet}}</small></h1></a>
<h4>Tweet #: {{id}}</h4>
<div><br>
  Maybe some tweets?:<br> {{#if content}}
    {{content}}
  {{else}}
    tweets are for the week.
  {{/if}}
</div>
