<h1 class="prettytweets panel panel-default"> <small></h1>
  <h1><div class="panel-body well well-sm">
  {{#if user.email}}
    @{{user.email}}
    {{else}}
      You
    {{/if}}
  🗣:

  {{content}}</small></div></h1></div>
